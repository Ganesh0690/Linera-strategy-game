services:
  app:
    build: .
    ports:
      - "5173:5173"
      - "8080:8080"
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:5173"]
      interval: 10s
      timeout: 5s
      retries: 30
